---
instances:
    - name: appserver
      type: appserver
    - name: database
      type: database
    - name: loadbalancer
      type: loadbalancer
    - name: monitoring
      type: monitoring

relative_names:
    - name: www # www.<domain>
    - name: "@" # so <domain> works as a URL. Otherwise only www.<domain> would work.
